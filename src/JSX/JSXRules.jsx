import React from 'react'

const JSXRules = () => {
  return (
    <div id = "JSx-rule">
        <h1>JSX Rules</h1>
        <p>JSX must return a singleparent element</p>
        <p>JSX element must be proprely closed</p>
        <p className='my-class'>JSX atrribute must be written using camelCase (e.g. className instead of class)</p>
    </div>
  )
}

export default JSXRules